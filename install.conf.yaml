- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    #~/.dotfiles: ''
    ~/.zshrc: zshrc
    ~/.bashrc: bashrc
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/bin/ack: tools/ack
    ~/bin/z.sh: tools/z/z.sh

- shell:
  - [git submodule update --init --recursive, Installing submodules]

# Other things to rememeber:
#    sudo apt-get install silversearcher-ag # For 'ag' which is a fast 'ack' ;-)
#    ~/.dotfiles/tools/fzf/install # To download the right binary
